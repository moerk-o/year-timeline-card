## Fixed

- Fixed nested layout compatibility (layout-card, mod-card, grid containers)
- Added `:host { display: block; width: 100% }` to all child components
- Child components (timeline-bar, facts-block, marker-list) now properly inherit width in flex/grid contexts
