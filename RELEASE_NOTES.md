## Fixed

- Fixed nested layout compatibility (layout-card, mod-card, grid containers)
- Added CSS variable fallbacks to all child components for broken inheritance chains
- Child components now define their own CSS variables with HA theme fallbacks
